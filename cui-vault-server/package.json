{
  "name": "cui-vault-server",
  "version": "1.0.0",
  "description": "CUI Vault API server - FIPS boundary for CUI file storage (POST/GET/DELETE /v1/files)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "init-db": "node scripts/init-db.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.0.2",
    "pg": "^8.11.3"
  }
}
